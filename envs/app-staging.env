# Authenticated client-app staging (dev-app)
QA_ENV=app-staging
BASE_URL=https://dev-app.helloalex.ai

# Credentials: set in process env or local .env (never commit passwords)
# QA_APP_EMAIL=
# QA_APP_PASSWORD=
